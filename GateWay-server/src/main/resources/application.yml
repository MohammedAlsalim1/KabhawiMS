spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      httpclient:
        max-header-size: 8KB
  codec:
    max-in-memory-size: 100MB

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://discovery-server:8761/eureka/}